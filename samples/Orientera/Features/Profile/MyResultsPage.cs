namespace Orientera.Features.Profile;

/// <summary>
/// The runner's own season. A subpage of Jag rather than a section of the app: a result belongs
/// to the competition it was run in, and this list is the way back to those competitions.
/// </summary>
[NavigableRegion(Title = "Mina resultat")]
public partial class MyResultsPage { public MyResultsPage() => InitializeComponent(); }
