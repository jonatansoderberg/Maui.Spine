<SpinePage
    xmlns="http://schemas.microsoft.com/dotnet/maui/global"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Orientera.Features.Events.ChooseClassSheet"
    x:TypeArguments="ChooseClassSheetViewModel"
    x:DataType="ChooseClassSheetViewModel">

    <Grid RowDefinitions="Auto,*">

        <Label Grid.Row="0"
               Style="{StaticResource CaptionLabel}"
               Padding="16,0,16,10"
               Text="Klassen styr banan, startlistan och prediction." />

        <CollectionView Grid.Row="1" Margin="16,0,16,16" ItemsSource="{Binding Classes}" SelectionMode="None">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="6" />
            </CollectionView.ItemsLayout>

            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <Border Style="{StaticResource Card}" Padding="16,12">
                        <Label Style="{StaticResource BodyStrongLabel}" Text="{Binding .}" />

                        <Border.GestureRecognizers>
                            <TapGestureRecognizer
                                Command="{Binding Source={RelativeSource AncestorType={x:Type CollectionView}}, Path=BindingContext.ChooseCommand}"
                                CommandParameter="{Binding .}" />
                        </Border.GestureRecognizers>
                    </Border>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </Grid>

</SpinePage>
