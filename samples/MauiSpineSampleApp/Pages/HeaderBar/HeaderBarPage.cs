namespace MauiSpineSampleApp.Pages.HeaderBar;

// Every header setting at its default: a normal layout, no large title, the Auto background. The
// footer changes them on the view model while the page is shown, the same properties the attribute sets.
[NavigableRegion(Title = "Header bar")]
public partial class HeaderBarPage { public HeaderBarPage() => InitializeComponent(); }
