<SpinePage
    xmlns="http://schemas.microsoft.com/dotnet/maui/global"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="MauiSpineSampleApp.Pages.Menus.MenusPage"
    x:TypeArguments="MenusPageViewModel"
    x:DataType="MenusPageViewModel">

    <ScrollView>
        <VerticalStackLayout Padding="20,16" Spacing="10">
            <PackageChips Packages="Plugin.Maui.Spine, Plugin.Maui.Spine.Svg.Icons" />

            <Label Text="The header action (top right) opens a native menu: a titled section with a picker, a submenu with toggles that keep the menu open, and a destructive row. On iOS 26 the glass button morphs into the menu."
                   FontSize="14" />

            <Label Text="{Binding LastPick}" FontSize="16" FontAttributes="Bold" />
            <Label Text="{Binding Summary}" FontSize="14" />

            <Label Text="Pop-up button: the text follows the pick" FontAttributes="Bold" Margin="0,12,0,0" />
            <HorizontalStackLayout Spacing="12">
                <Button Text="Name" Glass.Style="Regular" TextColor="{AppThemeBinding Light=Black, Dark=White}"
                        MenuButton.Items="{Binding SortMenu}" MenuButton.ShowsSelection="True" />
                <ImageButton SvgImageSource.Svg="more.svg" SvgImageSource.Padding="10" Glass.Style="Regular"
                             WidthRequest="44" HeightRequest="44" MenuButton.Items="{Binding MoreMenu}" />
            </HorizontalStackLayout>

            <Border StrokeThickness="0" BackgroundColor="#22808080" Padding="10,8" StrokeShape="RoundRectangle 8" Margin="0,8,0,0">
                <Label FontSize="11" FontFamily="{OnPlatform iOS=Menlo, MacCatalyst=Menlo, Android=monospace, WinUI=Consolas}"
                       Text="PageActions.Add(new PageAction(null, new MenuItems&#10;{&#10;    new MenuSection(&quot;Filter:&quot;)&#10;    {&#10;        new MenuPicker(PickCommand)&#10;        {&#10;            new MenuAction(&quot;All items&quot;, &quot;house.svg&quot;) { IsChecked = true },&#10;            new MenuAction(&quot;Favourites&quot;, &quot;done.svg&quot;),&#10;        },&#10;    },&#10;    new MenuSection&#10;    {&#10;        new SubMenu(&quot;Media types&quot;) { photos, videos },   // KeepsMenuOpen toggles&#10;        new MenuAction(&quot;Reset&quot;, &quot;refresh.svg&quot;, ResetCommand) { IsDestructive = true },&#10;    },&#10;}) { Svg = &quot;more.svg&quot; });&#10;&#10;&lt;Button Text=&quot;Name&quot; Glass.Style=&quot;Regular&quot;&#10;        MenuButton.Items=&quot;{Binding SortMenu}&quot; MenuButton.ShowsSelection=&quot;True&quot; /&gt;" />
            </Border>
        </VerticalStackLayout>
    </ScrollView>

</SpinePage>
