<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
    <!-- See https://aka.ms/maui-publish-app-store#add-entitlements for more information about adding entitlements.-->
    <dict>
        <!-- App Sandbox must be enabled to distribute a MacCatalyst app through the Mac App Store. -->
        <key>com.apple.security.app-sandbox</key>
        <true/>
        <!-- When App Sandbox is enabled, this value is required to open outgoing network connections. -->
        <key>com.apple.security.network.client</key>
        <true/>
        <!--
            No push entitlement and no App Group on Mac Catalyst. Both are restricted: an app signed with
            them but without a provisioning profile that grants them does not launch, and this sample
            has no Mac profile. Spine.Push gives local notifications without them; remote push on
            Catalyst follows the signing — name a profile with CodesignProvision and add
            aps-environment here. The App Group only serves the widget extension, which is iOS only.
        -->
    </dict>
</plist>

